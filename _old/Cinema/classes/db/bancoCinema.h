#ifndef BANCOCINEMA_H
#define BANCOCINEMA_H
/*
#include <sqlite3.h>
#include <string.h>

class bancoCinema
{
    public:
        bancoCinema();
        virtual ~bancoCinema();
        int execQuery(char query[]);

    protected:

    private:
        sqlite3 *db;
        char *zErrMsg;
        int rc;
        char *sql;
        int open();


};
*/
#endif // BANCOCINEMA_H
